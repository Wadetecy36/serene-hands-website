import Layout from "../components/Layout";import SectionHeading from "../components/SectionHeading";import Button from "../components/Button";import {ArrowDown} from "lucide-react";import {processSteps} from "../data/siteConfig";import {useSeo} from "../lib/useSeo";
export default function HowItWorks(){useSeo({title:"How It Works | Serene Hands",description:"See how the Serene Hands care journey begins, from your first conversation to personalized home care."});return <Layout><section className="hero-glow py-20 sm:py-28"><div className="serene-container text-center"><SectionHeading eyebrow="The care journey" title="Starting care should feel simple." description="You don’t need to have every answer before you reach out. We start with a conversation and take the next step together." align="center"/></div></section><section className="bg-cloud py-20 sm:py-28"><div className="serene-container"><div className="grid gap-0 border-y border-blush-deep lg:grid-cols-4">{processSteps.map((s,i)=><div key={s.number} className={`relative p-7 sm:p-9 ${i<3?"lg:border-r lg:border-blush-deep":""}`}><p className="serene-eyebrow text-blossom">{s.number}</p><h2 className="mt-5 text-3xl font-semibold">{s.title}</h2><p className="mt-3 text-sm leading-6 text-ink-soft">{s.description}</p>{i<3&&<ArrowDown className="mt-7 text-blossom lg:absolute lg:bottom-8 lg:right-6 lg:rotate-[-90deg]" size={18}/>}</div>)}</div></div></section><section className="bg-sage-soft py-20 sm:py-28"><div className="serene-container grid gap-8 lg:grid-cols-[1fr_auto] lg:items-center"><div><p className="serene-eyebrow text-sage">What happens next?</p><h2 className="mt-4 text-4xl font-semibold">You tell us what life looks like. We listen.</h2><p className="mt-5 max-w-2xl text-base leading-7 text-ink-soft">After you reach out, we review what you’ve shared, talk through your needs, and help you understand the next step. No need to know the perfect words.</p></div><Button to="/book" variant="sage" size="lg">Start the conversation</Button></div></section></Layout>}
